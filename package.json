{
  "name": "moral-letters",
  "version": "1.0.0",
  "description": "Seneca's Moral Letters to Lucilius — modern English, for slow reading",
  "scripts": {
    "bundle": "node scripts/bundle-client.mjs",
    "build": "npm run bundle && eleventy",
    "serve": "npm run bundle && eleventy --serve"
  },
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@instantdb/core": "^1.0.60",
    "esbuild": "^0.28.1"
  }
}
